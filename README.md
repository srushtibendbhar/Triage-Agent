# 🧠 Support Triage Agent (Multi-Domain)

This project is a simple terminal-based support triage agent built for the HackerRank Orchestrate hackathon.

The goal of this agent is to read support tickets and decide:
- what kind of issue it is
- which product it belongs to
- whether it can be answered safely
- or if it should be escalated to a human

It works across three domains:
- HackerRank
- Claude
- Visa


## 🚀 What this agent does

For every support ticket, the agent:

- Reads the issue and subject
- Understands the type of request (bug, feature request, etc.)
- Identifies the product area
- Checks if the issue is risky (like fraud or hacking)
- Decides whether to reply or escalate
- Generates a short and safe response
- Saves everything into a clean CSV file


## 🛠️ Tech Used

- Python
- Pandas


## ▶️ How to run



### Step 1: Install pandas
```bash
pip install pandas

Step 2: Run the agent

Go to the main project folder and run:

python code/main.py support_tickets/support_tickets.csv support_tickets/output.csv
Step 3: Check the output

The output will be saved in:

support_tickets/output.csv
📄 Output format

Each row in the output file contains:

status → replied or escalated
product_area → HackerRank / Claude / Visa Support
request_type → bug / feature_request / product_issue / invalid
response → short reply generated by the agent
justification → reason behind the decision
🧠 How I approached the problem

I used a simple rule-based approach instead of complex models.

I combined the subject and issue text
Then I used keywords to classify the request
(like "error", "failed", "help", etc.)
I checked for risky words such as:
fraud
hacked
unauthorized

If the issue looked risky, I escalated it.
Otherwise, I generated a safe and simple response.

I focused more on making the output clean and understandable rather than overly complicated.

🚨 Handling sensitive cases

Some issues should not be answered automatically.

For example:

fraud
unauthorized transactions
hacked accounts

These are always escalated to human support to avoid mistakes.

✅ What I focused on
Keeping the logic simple and clear
Making sure the output format is correct
Avoiding messy or confusing responses
Handling risky cases safely
🔮 What can be improved

If I had more time, I would:

Use the support documents more effectively
Improve classification accuracy
Handle multiple issues in one ticket
Make responses more context-aware
🏁 Final thoughts

This project helped me understand how a basic support triage system works.
I focused on building something that is simple, reliable, and easy to understand, while still meeting all the requirements of the problem.





